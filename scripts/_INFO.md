# 📁 Pasta de Scripts PowerShell

Todos os scripts `.ps1` do projeto estão organizados nesta pasta para facilitar a localização.

## 📋 Scripts Disponíveis

### 1. `sync_whatsapp_token.ps1`
Sincroniza o token do WhatsApp entre `config.json` e `whatsapp-bot/.env`

### 2. `corrigir_token_agora.ps1`
Script de correção urgente com diagnóstico completo

### 3. `verificar_deploy.ps1`
Script para verificar status do deploy

## 🚀 Como Usar

```powershell
# Executar qualquer script
.\scripts\sync_whatsapp_token.ps1
.\scripts\corrigir_token_agora.ps1
.\scripts\verificar_deploy.ps1
```

## 📖 Documentação Completa

Veja o arquivo `README.md` nesta pasta para informações detalhadas sobre cada script.
